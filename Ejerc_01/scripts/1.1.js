const nombre = "Jose Miguel"
let edad
edad = 20
const mascota = false
edad = "veinte"
mascota = true

console.log(nombre)
console.log(edad)
console.log(mascota)

console.log(`${nombre} tiene ${edad} años y ${mascota ? "tiene mascota" : "no tiene mascota"}`)